<%= simple_form_for(@bot) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :token %>
    <%= f.input :enable %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
